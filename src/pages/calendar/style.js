import styled from "styled-components";
import {theme} from "src/assets/theme";

export const Wrapper = styled.div`
    height: 100vh;
    display: flex;
    flex-direction: column;
    gap: 10%;
    background-color: ${theme.colors.Light_active};
`